# Topic 4 最小実装完了メモ

作成日: 2026-07-10

## 概要

Topic 4「強敵と階層メリハリ」は、ミニ悪魔ちゃんを使った最小実装まで完了した。

強敵をモンスター種の恒久属性にはせず、出現時の `encounterRank` として実装した。同じミニ悪魔ちゃんを5Fでは `strong`、将来の10F相当では `normal` として生成できる。

5F固定は動作確認しやすくするためのテーブル設定であり、汎用ロジックは階層番号5や `miniDevil` を知らない。

## 実装済み

- `normal` / `strong` の遭遇ランクプロファイル。
- ランク別のHP、攻撃、経験値補正。
- データ駆動の特殊遭遇テーブル。
- 汎用の特殊遭遇部屋選択と敵配置。
- 新規モンスター「ミニ悪魔ちゃん」。
- 専用SVG `assets/monster_mini_devil.svg`。
- 角、翼、尻尾を持つフォールバックCanvas形状。
- ミニ悪魔ちゃんの浮遊モーションと紫系のヒット粒子。
- 階開始、部屋発見、通常視認、強敵視認の敵別メッセージ。
- 遭遇entry、敵種、ランク共通、通常共通の順によるメッセージ解決。
- 強敵ランク共通のオーラ、警告マーク、HPバー装飾。
- 強敵用の予兆、遭遇、撃破SE。
- 強敵撃破時の専用ログ、浮遊文字、フラッシュ、カメラ揺れ。
- 強敵の経験値補正と確定アイテムドロップ。
- `state.stats.strongDefeated` とゲームオーバー画面への表示。
- デバッグURLによる階層、遭遇位置、想定装備の確認。

## データ上のMVP設定

- 階層: 5F。
- 出現率: 100%。
- 敵種: `miniDevil`。
- 遭遇ランク: `strong`。
- 強敵部屋: 開始部屋、階段部屋を除く5×4以上の部屋。
- 素のミニ悪魔ちゃん: HP `10 + 階層補正`、攻撃 `5 + 階層補正`、経験値10。
- 5F強敵個体: HP21、攻撃8、経験値18。
- 確定報酬: 薬草40%、鉄の剣35%、おにぎり25%。

## 配置フォールバック

開始部屋と階段部屋以外に有効な部屋がない場合、強敵を無理に置かない。

2部屋のフォールバックマップで階段部屋へ配置すると撃破必須になり、「避ける」選択を壊すためである。進行可能性を優先し、そのフロアでは特殊遭遇をスキップする。

200件のシード固定レイアウトで配置率を監視し、75%以上をテスト基準としている。現在の5×4以上の候補条件では200件中200件で配置できた。

## メッセージ追加方法

敵種の標準文言は `monster-catalog.js` の `messages` に置く。

```js
messages: {
  floorPresence: "階開始時の予兆",
  roomPresence: "部屋発見時の予兆",
  encounter: "通常個体の初回視認",
  strongEncounter: "強敵個体の初回視認",
}
```

特定の遭遇だけ文言を変える場合は、特殊遭遇entryの `messages` で上書きする。

優先順位:

1. 特殊遭遇entry。
2. モンスターカタログ。
3. 遭遇ランク共通。
4. 通常遭遇共通。

## 確認済み

- `node --check` が全変更JSで通過。
- 既存 `structure-tests.js` が通過。
- `monster-system-tests.js` が通過。
- `special-encounter-tests.js` が通過。
- 既存ゴーレムの5F生成値が変更されていない。
- 5F `strong` のミニ悪魔ちゃんがHP21、攻撃8、経験値18になる。
- 10F `normal` のミニ悪魔ちゃんがHP20、攻撃10、経験値10になる。
- 出現階を7F、敵種をコウモリへ変えたテーブルでも同じ配置処理が動く。
- 遭遇固有メッセージが敵種標準文言より優先される。
- 未定義メッセージが共通テンプレートへフォールバックする。
- ブラウザの5F画面で専用SVG、強敵部屋、オーラ、敵別ログを確認。
- ブラウザのレベル3＋鉄の剣条件で、2回の攻撃、5ダメージの消耗で撃破を確認。
- 撃破時に経験値18と薬草1個のドロップを確認。
- 無装備レベル1では強敵に倒され、ミニ悪魔ちゃん専用死因が表示されることを確認。
- ゲームオーバー画面に強敵撃破数が表示されることを確認。
- ブラウザコンソールにerror/warningがないことを確認。

## デバッグ確認方法

ローカル配信時だけ、URLクエリで確認状態を作れる。

- `?debug=1&floor=5`: 5Fから開始。
- `?debug=1&floor=5&encounter=1`: 強敵の近くから開始。
- `?debug=1&floor=5&encounter=1&loadout=1`: レベル3、鉄の剣装備で強敵の近くから開始。

通常URLではデバッグ開始位置や装備変更は有効にならない。

## 残した判断

- 5Fの100%出現はMVP検証設定。体感確認後に出現率や階層をデータで変更してよい。
- ミニ悪魔ちゃんは強敵専用画像にせず、将来の通常敵化を前提にした。
- 強敵専用表現は本体SVGではなく、遭遇ランク側のCanvas演出へ置いた。
- プレイヤーの現在HPによって報酬ランクを変えない。ランクは生成時に確定する。
- 特殊AI、遠距離攻撃、状態異常はTopic 6へ残した。
- 新しい強敵専用アイテムはTopic 5へ残した。
- 強敵BGMは追加せず、Web Audioの短い専用SEに留めた。

## 次Topicへの引継ぎ

- Topic 5では `strongDefault` 報酬へレア武器、盾、巻物を追加できる。
- Topic 6ではミニ悪魔ちゃんへ固有AIを追加できる。
- 新しい敵を浅層の強敵として先行登場させ、深層で通常敵化する流れを再利用できる。
- 10Fを正式実装する際は、通常敵テーブルへ `{ type: "miniDevil", rank: "normal" }` を追加する。
- 出現候補を複数にする場合は、特殊遭遇entryを重み付きで追加するだけでよい。

## 関連ドキュメント

- `strong-enemy-floor-pacing-plan.md`
- `../prot2_topic3/topic3-completion-handoff.md`
- `../prot2_topic2/step6-addition-template-handoff-plan.md`
- `../mid-term-topic-roadmap.md`
